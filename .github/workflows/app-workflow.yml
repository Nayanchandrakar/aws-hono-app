name: Testing github actoins

on: 
    push:
      branches: 
        - master

    pull_request: 
      branches:
        - master


jobs:
  run-scripts:
      runs-on: ubuntu-latest
      steps: 
        - name: Set up Node.js
          uses: actions/setup-node@v4
        - name: Run a node js script
          run: | 
            echo "Running a Node Script"
            node -e "console.log("Hello world")"