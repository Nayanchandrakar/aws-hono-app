name: Hello world app

on:
    push:
        branches: 
            - master

    pull_request: 
        branches: 
            - master

jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Print hello world
              run: echo "Hello, Github actions"